# Node
node_modules/
npm-debug.log*
yarn-error.log

# Build output
dist/
build/

# Env / secrets
.env
.env.local
.env.*.local

# PM2 ecosystem config — operator-edited, host-specific (cwd, log paths).
# Checked-in sample at ecosystem.config.sample.cjs. .js form covered too in
# case an operator carves out CommonJS for it via a local package.json.
ecosystem.config.cjs
ecosystem.config.js

# Runtime data — per-chatbot regenerated system blocks, anything that's
# host/instance-specific. Operator-edited templates live under templates/
# and ARE version controlled.
data/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Test coverage
coverage/
.nyc_output/

# Temp / scratch
tmp/
var/
*.swp

# Inspiration / reference assets pulled in from other projects.
# Kept on disk while we're using them, never committed.
dev-notes/verify-trusted-api-default-route.png
